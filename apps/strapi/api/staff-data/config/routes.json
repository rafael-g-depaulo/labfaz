{
  "routes": [
    {
      "method": "GET",
      "path": "/staff-data",
      "handler": "staff-data.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/staff-data",
      "handler": "staff-data.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/staff-data",
      "handler": "staff-data.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
